import React from "react";
import Menu from "./Menu";

const MenuList = () => {
  return (
    <section className='menu-container'>
      <header>
        <h4>Menu</h4>
      </header>
      <Menu />
    </section>
  );
};

export default MenuList;
